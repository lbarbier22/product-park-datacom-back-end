spring:
  datasource:
    url: jdbc:h2:mem:datacom-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

datacom:
  jwt:
    secret: test-secret-uniquement-pour-les-tests-jamais-en-prod-0123456789
    expiration-ms: 3600000
